name: Final Build APK
on: [workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ArtemSashka/buildozer-action@v1
        with:
          buildozer_version: master
          
